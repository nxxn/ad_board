<% if current_user && current_user == @user %>
  <%= render partial: 'non_public_profile', locals: { user: @user } %>
<% else %>
  <%= render partial: 'public_profile', locals: { user: @user } %>
<% end %>
